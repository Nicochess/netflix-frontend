import { layout } from "./layout"

const core = {
    primary: "#E13A13",
    secondary: "#1A1A1A",
}

const typography = {
    primary: "#FFF",
    contrast: "#868D96"
}

const border = {
    default: "#E0E0E0"
}

export const pallete = {
    core,
    typography,
    border
}